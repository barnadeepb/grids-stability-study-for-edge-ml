# Raw dataset (downloaded by scripts/download_data.py, not tracked in git)
data/

# Python environment / caches
.venv/
__pycache__/
*.pyc

# Local tooling config, not part of the project
.claude/

# Manuscript drafting (Word doc + build tooling) -- not part of the
# public code artifact, kept out of the repo intentionally.
paper/
node_modules/
package.json
package-lock.json

# Hugging Face release artifact (trained model + model card) -- staged
# locally until credentials are available to publish it to Hugging Face;
# not part of the public GitHub repo.
huggingface_release/
